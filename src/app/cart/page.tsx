import { CartSection } from '@/components/sections/cart-section';

export const metadata = {
  title: 'Cart',
  description: 'Premium shopping cart experience for NexaCart.',
};

export default function CartPage() {
  return <CartSection />;
}
