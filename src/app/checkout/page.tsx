import { CheckoutSection } from '@/components/sections/checkout-section';

export const metadata = {
  title: 'Checkout',
  description: 'Premium checkout experience for NexaCart.',
};

export default function CheckoutPage() {
  return <CheckoutSection />;
}
